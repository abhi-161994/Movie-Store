
			<nav class="navbar navbar-default" role="navigation">
				<div class="navbar-header">
					</div>

				<div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
					<ul class="nav navbar-nav">
						<li><button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
						</button><a class="navbar1" href="<%= root_path%>">Movie </a></li>
						<li class="active">
	        		<a class="navbar1" href="<%= root_path%>">Home</a>
	      		</li>
					</ul>

					<%= form_tag(movies_detail_path, :method => "get", id: "search-form",class:"navbar-form navbar-left") do %>
				  <span class="glyphicon glyphicon-search seacrchs"></span>
						<%= text_field_tag :search, params[:search], placeholder: "Search Movies",class:"form-group form-control" %>
						<%= submit_tag "Search",class:"btn btn-default sec_bu" %>
						<% end %>


					<ul class="nav navbar-nav navbar-right">
						<li>
							<% if user_signed_in? %>

							<% else %>
							<li><a class="navbar1"  target="_blank" href="<%= new_user_registration_path%>"> Sign Up</a></li>
							<li><a class="navbar1" href="<%= new_user_session_path%>"<span class="glyphicon glyphicon-user"></span> Login in</a></li>
							<% end %>
						</li>

						<li class="dropdown">
							<% if user_signed_in? %>
							<% if current_user.image_url.present? %>
             <img class ="pull-left" style="width:50px;border-radius: 35px;border: 3px solid #cccccc; height: 50px;" src="<%=current_user.image_url%>"/>
							 <a  href="#" class="dropdown-toggle navbar-login" data-toggle="dropdown" style="float: left;"></span><%= current_user.name%>
							 <% else %>
							 <img class="pull-left" style="width:50px;border-radius: 35px;border: 3px solid #cccccc; height: 50px;" src="/assets/avatar.png"/>
							 <a  href="#" class="dropdown-toggle navbar-login" data-toggle="dropdown" style="float: left;"></span><%= current_user.name%>
							 <% end %>
							 <% end %>
						  	<ul class="dropdown-menu">
									 <% if user_signed_in? %>
								<li>
									<%= link_to "User Profile",edit_user_registration_path ,class:"drpdown",target:"_blank"%>
								</li>
								<li>
									 <%= link_to "SignOut",destroy_user_session_path, method: :delete,class:"drpdown"%>
								</li>
								<% else %>



						    <% end %>
							</ul>
						</li>

					</ul>
				</div>

			</nav>
